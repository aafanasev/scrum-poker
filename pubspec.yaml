name: scrum_poker
description: Scrum Poker

version: 1.0.6+6

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: any

flutter:
  uses-material-design: false
  fonts:
    - family: Alata
      fonts:
        - asset: fonts/Alata-Regular.ttf
